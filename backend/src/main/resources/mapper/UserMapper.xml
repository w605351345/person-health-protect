<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personhealth.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.personhealth.entity.User">
        <id column="id" property="id"/>
        <result column="phone" property="phone"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="status" property="status"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="last_login_ip" property="lastLoginIp"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, phone, password, nickname, avatar, status,
        last_login_time, last_login_ip, create_time, update_time
    </sql>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{id}
    </select>

    <select id="findByPhone" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE phone = #{phone}
    </select>

    <insert id="insert" parameterType="com.personhealth.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (phone, password, nickname, avatar, status, create_time, update_time)
        VALUES (#{phone}, #{password}, #{nickname}, #{avatar}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.personhealth.entity.User">
        UPDATE user
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateLastLogin">
        UPDATE user
        SET last_login_time = NOW(), update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>
